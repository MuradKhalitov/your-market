# Location catalogue notes

No GAR/FIAS generator or importer is used by this project. The product catalogue is edited directly in `src/main/resources/location-catalog.yaml` through pull request review, in the same way as `vehicle-catalog.yaml`.
